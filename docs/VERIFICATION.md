# Initial verification checklist

This file exists to force the initial template through the pull-request CI workflow.

The CI workflow must compile the Android template, run lint, and verify both debug APK and release AAB artifacts.
